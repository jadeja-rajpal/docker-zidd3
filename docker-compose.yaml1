services:

  dashboard:
    build: .
    container_name: devops-dashboard

    ports:
      - "3000:3000"

    volumes:
      - dashboard-data:/app/data

    restart: unless-stopped


volumes:
  dashboard-data:
